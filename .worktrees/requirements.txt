# ClamUI Python Dependencies
# Note: GTK4 and libadwaita system libraries must be installed separately:
# sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-4.0 gir1.2-adw-1 libadwaita-1-dev

# GTK4 Python bindings
PyGObject>=3.48.0

# Cairo graphics library for Python
pycairo>=1.25.0
