# Runtime dependencies for ClamUI Flatpak build
# Used by flatpak-pip-generator to generate python3-runtime-deps.json
#
# Note: PyGObject and pycairo are provided by org.gnome.Platform runtime
# Note: pygobject-stubs are type stubs, not needed at runtime
psutil>=5.9.0
matplotlib>=3.8.0
requests>=2.31.0
urllib3>=2.6.3
keyring>=25.0.0
